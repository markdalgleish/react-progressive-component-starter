{
  "name": "react-progressive-component-starter",
  "version": "1.0.0",
  "description": "Starter kit for progressive React component projects",
  "main": "lib/index.js",
  "scripts": {
    "babel": "babel src --out-dir lib",
    "webpack": "webpack",
    "build": "npm run babel && npm run webpack",
    "start": "rm -rf demo-dist && http-server ./demo-dist -p 8000 & npm run babel -- --watch & npm run webpack -- --watch",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mark Dalgleish",
  "license": "MIT",
  "dependencies": {
    "react": "^0.12.2"
  },
  "devDependencies": {
    "babel": "^4.0.1",
    "babel-core": "^4.0.2",
    "babel-loader": "^4.0.0",
    "file-webpack-plugin": "git://github.com/markdalgleish/file-webpack-plugin",
    "http-server": "^0.7.4",
    "require-without-cache": "0.0.6",
    "webpack": "^1.5.3"
  }
}
